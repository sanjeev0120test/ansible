This folder is optional. The supported workflow is WSL bash only — see WSL_COMMANDS.txt in the repo root. Use Run-FromCursor.ps1 here only if you cannot use WSL as the default
Cursor terminal (e.g. locked-down Windows policy).

Run from repo root in PowerShell:
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deprecated-windows/Run-FromCursor.ps1
